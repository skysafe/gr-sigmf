# auto-generated by grc.converter

id: sigmf_usrp_gps_message_source
label: USRP GPS Message Source
category: '[SigMF]'

parameters:
-   id: uhd_args
    label: UHD Args
    dtype: string
-   id: poll_interval
    label: Poll Interval (s)
    dtype: real
    default: '1.0'

outputs:
-   domain: message
    id: out

templates:
    imports: import gr_sigmf
    make: gr_sigmf.usrp_gps_message_source(${uhd_args}, ${poll_interval})

file_format: 1
